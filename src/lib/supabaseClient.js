// Re-exports the existing Supabase client. The actual client config lives in
// src/supabaseClient.js (unchanged from before). This keeps imports tidy under lib/.
export { supabase } from "../supabaseClient";
